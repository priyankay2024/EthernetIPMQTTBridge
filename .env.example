# EthernetIP Configuration
ETHERNETIP_HOST=127.0.0.1
ETHERNETIP_SLOT=0
ETHERNETIP_TAGS=Temperature,Pressure,Speed,Status

# MQTT Configuration
MQTT_BROKER=broker.hivemq.com
MQTT_PORT=1883
MQTT_TOPIC_PREFIX=ethernetip/test/
MQTT_CLIENT_ID=ethernetip_bridge_simulator

# Polling Configuration
POLL_INTERVAL=10.0

# Notes:
# - For simulator mode, keep ETHERNETIP_HOST as 127.0.0.1
# - For real PLC, set ETHERNETIP_HOST to your PLC's IP address (e.g., 192.168.1.100)
# - ETHERNETIP_TAGS should be comma-separated tag names
# - MQTT_BROKER can be any public or private broker
# - POLL_INTERVAL is in seconds (how often to read tags)
